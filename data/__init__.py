"""Dataset definition: loaders (real, licence-checked) and generators (synthetic).

A package only so the backend test suite and `app.db.seed` can import the deliberate
scenario specification. Nothing here is shipped in the application wheel.

Owner: Stream C.
"""
